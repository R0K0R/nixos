{
  "$comment": [
    "nixd workspace config: gives completion and hover for `my.*` in host files,",
    "resolved from the real evaluated option set rather than guessed.",
    "",
    "RUN NIXD WITH IFD DISABLED. Pointing it at a full host eval otherwise lets",
    "nix-doom-emacs-unstraightened's import-from-derivation trigger a",
    "doom-intermediates build from the editor -- once measured at 226 minutes,",
    "and it fires on file open, not on demand. Add to the server invocation:",
    "  --option allow-import-from-derivation false",
    "The only thing lost is completion inside features/emacs.",
    "",
    "galaxybook4-pro360 is used as the reference host because it enables nearly",
    "every feature, so nearly every option is reachable. Options a host does not",
    "enable are still DECLARED -- registration is unconditional -- so they",
    "complete regardless."
  ],

  "options": {
    "nixos": {
      "expr": "(builtins.getFlake \"/home/r0k0r/flakes/nixos\").nixosConfigurations.galaxybook4-pro360.options"
    },
    "home-manager": {
      "expr": "(builtins.getFlake \"/home/r0k0r/flakes/nixos\").nixosConfigurations.galaxybook4-pro360.options.home-manager.users.type.getSubOptions []"
    }
  },

  "nixpkgs": {
    "expr": "import (builtins.getFlake \"/home/r0k0r/flakes/nixos\").inputs.nixpkgs { }"
  }
}
